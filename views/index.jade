extends layout

block content
  each book in books
    div
      div
        img(src="https://librarynodeapp.blob.core.windows.net/covers/" + book.bookId + ".jpg")
      h3=book.title
      h4=book.author
